<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vitacheck.vitacheck">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <application
        android:name="vitacheck.vitacheck.fragments.ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_home"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".fragments.DoctorActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".fragments.VitalSignsActivity" />
        <activity
            android:name=".fragments.MedicineActivity"
            android:label="@string/title_activity_medicine"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".fragments.DietFoodActivity"
            android:label="DietFoodActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".fragments.DietRecipeActivity"
            android:label="DietRecipeActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".fragments.DietHealthArticleActivity"
            android:label="DietHealthArticleActivity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".fragments.ProfileActivity"
            android:label="Profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".fragments.MedicineActivityAlarmPopup"
            android:theme="@style/AppTheme.AlarmPopup"></activity>
    </application>


</manifest>
